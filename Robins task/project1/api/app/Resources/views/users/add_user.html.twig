{% extends 'base.html.twig' %}

{% block body %}

{#
    <form method="post" action="{{ path("add_user") }}">
        <div>
            <label>First Name</label>
            <input type="text" class="form-control" id="exampleInputEmail1" placeholder="first name" value="$data['user']['first_name']" name="user[first_name]">
        <div>
        
        <div>
            <label>Last Name</label>
            <input type="text" class="form-control" id="exampleInputEmail1" placeholder="last name" name="user[last_name]">
        <div>
        
        <label>Color: </label>
        <select id="disabledSelect" class="form-control" name="user[color_id]">
            {% for color in colors %}
                <option value="{{ color.id }}">{{ color.name }}</option>
            {% endfor %}
        </select><br/>
        
        <input type="submit" value="Add to db" />
    <form>
    
    #}
    
    <h1>Add user:</h1>
    <div class="serchSingle col-lg-6 col-md-6 col-sm-6 col-xs-6">
        <div class="row">
            {{ form_start(userForm) }}
            {{ form_widget(userForm, { 'attr': {'class': 'calendar'} }) }}
            <input type="submit" class="btn btn-default" value="Add to bd" />
            {{ form_end(userForm) }}
        </div>
    </div>
    
    
    
{% endblock %}
